# Copy thành .env khi chạy local (không commit .env)
PORT=3000
NODE_ENV=development

ADMIN_EMAIL=baohiem@mo
ADMIN_PASSWORD=123456
SESSION_SECRET=doi-chuoi-bi-mat-dai-hon-32-ky-tu

# Render production (bắt buộc trên dashboard Render):
# NODE_ENV=production
# COOKIE_CROSS_SITE=true
# ADMIN_EMAIL, ADMIN_PASSWORD, SESSION_SECRET — đặt giá trị riêng, không dùng mặc định

# Tùy chọn CORS (domain tùy chỉnh, phân tách bằng dấu phẩy)
# *.vercel.app và *.onrender.com đã được cho phép sẵn trong script.js
# CORS_ALLOWED_ORIGINS=https://web-baohiem.vercel.app
# CORS_ALLOW_ALL=false
